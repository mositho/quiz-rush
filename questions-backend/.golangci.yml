linters-settings:
  errcheck:
    check-type-assertions: true
    check-blank: true
  govet:
    shadow: true
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - gosec
    - unused
run:
  timeout: 5m
  issues-exit-code: 1
